var React = require('react');
var ReactDOM = require('react-dom');
var CharacterSheet = require('./character_sheet')

ReactDOM.render(
  <CharacterSheet/>,
  document.getElementById('react-container')
);